function TurinMachine(input) {
    let tape = [...input]
    let headPosition = 0;
    let state = "q0";
  
    while (this.headPosition < tape.length) {
      let symbol = tape[headPosition]
  
      switch (state) {
        case 1:
      }
    }
  }